import { SkeletonUiDirective } from './skeleton-ui.directive';

describe('SkeletonUiDirective', () => {
  it('should create an instance', () => {
    const directive = new SkeletonUiDirective();
    expect(directive).toBeTruthy();
  });
});
